<controls:MetroWindow x:Class="GestureSign.ControlPanel.Dialogs.SelectPresetDialog"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:localization="clr-namespace:GestureSign.Common.Localization;assembly=GestureSign.Common"
                      mc:Ignorable="d"
                      Title="{localization:LocalisedText SelectPresetDialog.Title}"
                      Width="350"
                      Height="400"
                      FlowDirection="{StaticResource DefaultFlowDirection}"
                      BorderBrush="{StaticResource MahApps.Brushes.Accent}"
                      BorderThickness="1"
                      ResizeMode="NoResize"
                      WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{localization:LocalisedText SelectPresetDialog.SelectPreset}"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <ListBox x:Name="PresetsListBox"
                 Grid.Row="1"
                 SelectionMode="Single"
                 MouseDoubleClick="PresetsListBox_MouseDoubleClick"
                 BorderThickness="1"
                 BorderBrush="{DynamicResource MahApps.Brushes.Gray7}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5,3">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding DisplaySummary}"
                                   FontSize="11"
                                   Foreground="{DynamicResource MahApps.Brushes.Gray3}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="CancelButton"
                    Content="{localization:LocalisedText Common.Cancel}"
                    FontSize="14"
                    Height="32"
                    Margin="0,0,5,0"
                    IsCancel="True" />
            <Button x:Name="OkButton"
                    Content="{localization:LocalisedText Common.OK}"
                    Click="OkButton_Click"
                    IsDefault="True"
                    FontSize="14"
                    Height="32"
                    Margin="5,0,0,0"
                    Grid.Column="1"
                    IsEnabled="False" />
        </Grid>
    </Grid>
</controls:MetroWindow>
