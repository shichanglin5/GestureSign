<Window x:Class="GestureSign.CorePlugins.ActivateApp.SelectWindowDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localization="clr-namespace:GestureSign.Common.Localization;assembly=GestureSign.Common"
        Title="{localization:LocalisedText CorePlugins.ActivateApp.SelectWindow}"
        Height="500"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView x:Name="WindowListView"
                  Grid.Row="0"
                  Margin="10"
                  SelectionMode="Single"
                  MouseDoubleClick="WindowListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{localization:LocalisedText ActionDialog.Icon}"
                                    Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Path=Icon}"
                                       Height="32"
                                       Width="32" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{localization:LocalisedText ApplicationDialog.ApplicationFileNameAndTitle}"
                                    Width="400">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Path=ProcessName}"
                                               FontWeight="Bold"
                                               Margin="0,2,0,2" />
                                    <TextBlock Text="{Binding Path=Title}"
                                               Foreground="Gray" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="10">
            <Button x:Name="OkButton"
                    Content="OK"
                    Width="75"
                    Height="25"
                    Margin="0,0,10,0"
                    Click="OkButton_Click"
                    IsDefault="True" />
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="75"
                    Height="25"
                    Click="CancelButton_Click"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
