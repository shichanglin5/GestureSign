<UserControl x:Class="GestureSign.Common.UI.MatchConditionList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:localization="clr-namespace:GestureSign.Common.Localization"
             xmlns:ui="clr-namespace:GestureSign.Common.UI"
             mc:Ignorable="d"
             d:DesignHeight="200"
             d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Application Path -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{localization:LocalisedText ApplicationDialog.ApplicationPath}"
                       Margin="0,0,0,5" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="ApplicationPathTextBox"
                         Height="23"
                         Grid.Column="0" />
                <Button x:Name="BrowseButton"
                        Content="..."
                        Width="30"
                        Grid.Column="1"
                        Margin="5,0,0,0"
                        Click="BrowseButton_Click" />
            </Grid>
        </StackPanel>

        <!-- Application Arguments (optional, controlled by ShowArguments) -->
        <StackPanel x:Name="ArgumentsPanel"
                    Grid.Row="1"
                    Margin="0,0,0,10"
                    Visibility="{Binding ShowArguments, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{localization:LocalisedText CorePlugins.ActivateApp.ApplicationArguments}"
                       Margin="0,0,0,5" />
            <TextBox x:Name="ApplicationArgumentsTextBox"
                     Height="23" />
        </StackPanel>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      MaxHeight="120">
            <StackPanel x:Name="ConditionPanel" />
        </ScrollViewer>

        <Button x:Name="AddButton"
                Grid.Row="3"
                Content="{localization:LocalisedText ApplicationDialog.AddCondition}"
                HorizontalAlignment="Left"
                Margin="0,5,0,0"
                Click="AddButton_Click" />
    </Grid>
</UserControl>
